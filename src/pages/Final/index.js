import React, {useState} from 'react';
import {View, Text, TextInput, Switch, Image, TouchableOpacity, ScrollView} from 'react-native';

export default function Final({nome, idade, estudante, limite}){


}